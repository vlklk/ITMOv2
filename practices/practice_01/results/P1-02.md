# P1-02 — ответ модели (тот же diff + Master Prompt v1)

- Инструмент и модель: OpenCode, `opencode/big-pickle` (та же модель, что в P1-01)
- Вход: `TRAINING_PR.diff` + Master Prompt v1 со встроенным Context Pack, историю рабочего чата модель не получала
- Запуск: отдельная временная папка вне репозитория, агент `plan`, команда
  `opencode run --agent plan --model opencode/big-pickle --file TRAINING_PR.diff -- "<Master Prompt v1>"`
- Строка журнала: [`prompts.md`](../prompts.md), P1-02

## Ответ модели

```json
{
  "summary": "Добавлен POST-эндпоинт /api/reviews (app/api.py), который передаёт payload[\"diff\"] в новый метод review() сервиса app/review_service.py и возвращает {comment: ...}. Diff уходит во внешний LLM как есть: нет удаления секретов, нет лимита длины, нет таймаута и обработки ошибок вызова.",
  "risks": [
    {
      "file": "app/review_service.py",
      "line": 14,
      "evidence": "prompt = f\"Review this pull request and find problems:\\n{diff}\"",
      "risk": "Полный diff передаётся во внешний LLM без удаления токенов, паролей и приватных ключей, что нарушает SEC-1."
    },
    {
      "file": "app/api.py",
      "line": 9,
      "evidence": "@app.post(\"/api/reviews\")\ndef create_review(payload: dict) -> dict[str, str]:\n    return review_service.review(payload[\"diff\"])",
      "risk": "Входной diff никак не ограничен: длина более 20000 символов не отклоняется с HTTP 413, что нарушает API-1."
    },
    {
      "file": "app/review_service.py",
      "line": 15,
      "evidence": "answer = self.llm.generate(prompt)",
      "risk": "Вызов внешнего LLM выполняется без таймаута в 10 секунд, а исключение не превращается в контролируемый ответ (возможен 500), что нарушает REL-1."
    }
  ],
  "checks": [
    "Создать LLM-заглушку, записывающую полученный prompt; передать в POST /api/reviews diff с тестовым token (например, «token=abc123») и убедиться, что в prompt токен заменён на [REDACTED] (сейчас проверка упадёт).",
    "Отправить на POST /api/reviews diff размером более 20000 символов и проверить, что возвращается HTTP 413 (сейчас проверка упадёт).",
    "Вызвать review() с LLM-заглушкой, которая ждёт дольше 10 секунд или бросает исключение, и убедиться, что API возвращает контролируемый ответ без 500."
  ]
}
```
