# Контекст AS IS и Context Pack

Файл ведёт OpenCode. Обсудите с агентом содержание и проверьте предложенный diff. Все дополнения и исправления поручайте агенту в чате.

## Продукт и команда сейчас

| Вопрос | Ответ | Источник или допущение |
|---|---|---|
| Какой продукт или сервис рассматриваем? | AI-помощник для code review PR. FastAPI-сервис с эндпоинтом `/api/reviews`, вызывающий внешний LLM для анализа diff | `TRAINING_PR.diff`, [`README.md`](README.md) |
| Кто им пользуется? | Разработчики, открывающие PR в репозитории. Сервис — внутренний инструмент для инженерной команды | Допущение по [`README.md`](README.md) |
| Как устроен текущий процесс? | При POST-запросе с `diff` сервис формирует промпт, отправляет в LLM и возвращает `{"comment": answer}` | `app/review_service.py`, `app/api.py` |
| Где возникает задержка или ошибка? | Нет валидации входа (`KeyError` при отсутствии `diff`), нет обработки ошибок LLM (timeout, недоступность), нет лимита размера diff | `app/api.py:36-37`, `app/review_service.py:20-22` |
| Какие системы и команды участвуют? | FastAPI-сервис, внешний LLM (через `LLM.generate`), репозиторий GitHub (пока не интегрирован) | `app/dependencies.py` (импорт `review_service`) |

## Context Pack для первого рабочего сценария

### Факты и правила

- `SEC-1`: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи
- `API-1`: diff длиннее 20 000 символов отклоняется с HTTP 413
- `REL-1`: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ
- `OUT-1`: ответ содержит `summary`, массив `risks` и массив `checks`. В `risks` максимум три элемента с полями `file`, `line`, `evidence`, `risk`
- `SCOPE-1`: сервис только советует; он не пишет код и не выполняет действия в GitHub
- `QA-1`: риск включается в ответ, только если он подтверждён строкой diff или правилом репозитория
- `OBS-1`: в лог пишутся только `request_id`, длительность и статус. Содержимое diff и ответа модели не логируется

### Формат входа и результата

- Вход: POST-запрос с JSON, содержащим ключ `diff` (строка с git diff)
- Результат: JSON с полями `summary` (строка), `risks` (массив объектов с `file`, `line`, `evidence`, `risk`), `checks` (массив строк)
- Максимум 3 риска в ответе

### Ограничения и запрещённые действия

- Сервис не может approve или merge PR (только рекомендации)
- Сервис не может писать код или выполнять действия в GitHub
- Нельзя логировать содержимое diff и ответа модели (только request_id, длительность, статус)
- Нельзя отправлять diff без предварительной очистки секретов

### Хороший пример

```text
app/review_service.py:16 — diff отправляется во внешний LLM без удаления секретов,
что нарушает SEC-1. Проверка: передать diff с тестовым token и убедиться,
что в prompt осталось [REDACTED].
```

### Плохой пример

```text
Код можно улучшить. Добавьте тесты и обработку ошибок.
```

### Что пока неизвестно

- Какой LLM используется (провайдер, модель)
- Есть ли уже интеграция с GitHub API
- Кто обслуживает сервис и кто принимает решения по ревью

## Как использовали AI

- Для чего: анализ TRAINING_PR.diff и поиск проблем в коде
- Тип промпта: zero-shot
- Строка в [`prompts.md`](prompts.md): P1-01
- Что проверил студент и какие исправления поручил агенту: проверили наличие правил SEC-1, API-1, REL-1, OUT-1 в diff — ни одно не реализовано
