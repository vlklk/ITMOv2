# Chain of Verification

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Проверяемый черновик или утверждение: Исходный файл `practices/practice_01/tests_load.md` содержит 2 сценария (10 rps / 60 сек и 50 параллельных) и текст-заглушку о том, что нагрузочные тесты станут актуальными после инкрементов 2–3. Черновик обновления должен кратно расширить покрытие тестов, убрать заглушки и обосновать метрики.

## Запрос на проверку

Сгенерируй расширенный план нагрузочного тестирования взамен `practices/practice_01/tests_load.md`, сохранив результат в `practices/practice_01/tests_load.md`.
Перед формированием итогового документа проведи проверку по шагам (Chain of Verification):
1. Сформулируй проверочные вопросы к полноте и реалистичности тестовой матрицы (профили нагрузки, SLA, ресурсы, Подтверждение).
2. Найди и проверь факты/критерии по каждому вопросу.
3. На основе верификации сформируй итоговый файл без заглушек и пропусков.

## Вопросы и результаты проверки

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| Достаточно ли 2 сценариев (baseline + burst) для оценки надежности системы? | Стандарты нагрузочного тестирования: smoke, baseline, capacity, peak, spike, stress, soak — минимальный набор для production-ready сервиса | Нет, требуются как минимум 7 базовых типов + специализированные сценарии (деградация LLM, read/write mix,граничный diff, параллельность) |
| Какие пороги latency (p95/p99) и error rate реалистичны для сервиса с внешним LLM timeout 10 сек (REL-1)? | context.md REL-1: timeout 10 сек; расчёт: p95 ≤ 8 сек (запас 20% до timeout), p99 ≤ 10 сек (= timeout), 5xx = 0 (REL-1 превращает ошибку в контролируемый ответ) | p95 ≤ 8 сек, p99 ≤ 10 сек, 5xx = 0%, 4xx только ожидаемые 413 (API-1) |
| Нужны ли отдельные сценарии для чтения/записи и mixed-трафика? | Паттерны веб-архитектур: read-heavy (80/20), write-heavy (20/80), mixed (70/30) — стандарт для API с CRUD | Да, необходимы раздельные профили: read-heavy (сценарий 8), write-heavy (сценарий 9), mixed (сценарий 10) |
| Какие сценарии специфичны для внешнего LLM-провайдера? | Анализ `app/review_service.py:20-21`: нет timeout, нет обработки ошибок LLM | Необходимы: slow LLM (сценарий 11), unavailable LLM (сценарий 12) — проверка контроля таймаутов и graceful degradation |
| Что должно входить в Подтверждение вместо обобщённого «Отчёт k6/locust»? | Best practices observability: метрики (k6 JSON), трейсы (APM/OpenTelemetry), логи (structured), дашборды (Grafana) | Каждый сценарий: k6/locust JSON + Grafana-снимок + APM-трейс по request_id (OBS-1) |
| Оправдана ли заглушка «нагрузочное тестирование станет актуальным после инкрементов 2–3»? | Требования к готовности продакшена: даже без timeout и логирования можно проверить стенд, граничные значения, отклонение oversized diff | Заглушку удалить; разрешить сценарии 1–2 и 14 (smoke, baseline, oversized diff) до инкрементов; полная матрица — после инкрементов 2–3 |
| Нужен ли soak-тест (длительный прогон) для выявления утечек памяти? | Стандартная практика: soak 4+ часа выявляет утечки FD, рост RSS, деградацию соединений | Да, soak 4 часа (сценарий 7) обязателен; порог: рост RAM ≤ 20% от старта |
| Как обосновать лимит CPU ≤ 70% в пике? | Capacity planning: 70% — безопасный порог для горизонтального масштабирования, выше начинается throttling | CPU ≤ 70% в пике, RAM рост ≤ 20% и стабилизация к концу soak |
| Нужен ли cold-start тест для сервиса с кэшем? | Паттерн: холодный старт после деплоя/restart даёт всплеск latency, важен для SLA | Да, cold-start сценарий (сценарий 15 из RCTF) с порогом: первый запрос ≤ 15 сек, P95 после прогрева ≤ 5 сек |
| Как проверить, что oversized diff не нагружает LLM? | API-1: diff > 20 000 → HTTP 413; проверка: 0 запросов к LLM (`external_llm_requests` = 0) | Сценарий 14: 100% HTTP 413, 0 запросов к LLM, p95 ≤ 0.5 сек |

## Исправленный результат

Результат сохранён в `practices/practice_01/tests_load.md`. Документ включает:
- Вводный блок с целями, инструкцией по применению и регламентом работы.
- SLO-таблицу с обоснованными порогами (p95 ≤ 8 сек, p99 ≤ 10 сек, 5xx = 0%, CPU ≤ 70%, RAM ≤ 20%).
- Матрицу из 15 нагрузочных сценариев (smoke, baseline, capacity, peak, spike, stress, soak, read-heavy, write-heavy, mixed, slow LLM, unavailable LLM, boundary diff, oversized diff, concurrency burst) с конкретными порогами и требованиями к Подтверждение.
- Разделы по инструментам (k6, Locust, Grafana+Prometheus, APM/OpenTelemetry, мок-LLM) и критериям приёмки (Pass/Fail).
- Блок «Готовность к запуску» с порядком разрешения сценариев по инкрементам.
- Матрицу покрытия рисков.

## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_01/tests_load.md` → переработан (структура документа, SLO-таблица, матрица сценариев, блок Подтверждение, критерии приёмки, матрица рисков).
- Изменение: на основе этапа верификации расширили покрытие с 2 до 15 сценариев, удалили текст-заглушку, зафиксировали пороги SLA/SLO с обоснованием по context.md (REL-1, API-1, OUT-1, OBS-1, SEC-1), определили состав Подтверждение для каждого сценария.
- Что отклонили: отклонили абстрактные формулировки «система выдерживает нагрузку», сценарии без явных числовых критериев, заглушку «нагрузочное тестирование станет актуальным после инкрементов 2–3» (заменена на условный порядок разрешения сценариев).
